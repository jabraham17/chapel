#!/usr/bin/env bash

set -eo pipefail

source $(cd $(dirname ${BASH_SOURCE[0]}) ; pwd)/../bash_helpers.bash

cd CompflagsBug

echo_and_run mason build -- -stestFlagCmdLine=1
echo_and_run mason run -- --testExecFlagCmdLine=bar
